@extends('layouts.customer')

@section('title', $transaction->kode_transaksi)
@section('header', $transaction->kode_transaksi)

@section('content')
<div class="mb-8"><a href="{{ route('customer.orders.index') }}" class="text-sm font-semibold text-brand-600">← Kembali ke pesanan</a><div class="mt-3 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="page-title">{{ $transaction->kode_transaksi }}</h1><p class="page-subtitle">Dibuat {{ $transaction->created_at->format('d F Y, H:i') }}</p></div><x-status-badge :status="$transaction->status_laundry"/></div></div>
<div class="grid gap-6 lg:grid-cols-3"><div class="card p-6 lg:col-span-2"><h2 class="font-bold text-slate-900">Detail laundry</h2><dl class="mt-6 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Layanan</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->layanan->nama_layanan }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Berat / jumlah</dt><dd class="mt-1 font-semibold text-slate-800">{{ (float) $transaction->berat }} {{ $transaction->layanan->satuan }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Tanggal masuk</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->tanggal_masuk->format('d F Y') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Estimasi selesai</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->tanggal_estimasi_selesai->format('d F Y') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Total tagihan</dt><dd class="mt-1 text-xl font-bold text-brand-600">Rp{{ number_format((float) $transaction->total_harga, 0, ',', '.') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Karyawan</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->karyawan?->nama_lengkap ?? 'Belum ditugaskan' }}</dd></div></dl>@if ($transaction->catatan)<div class="mt-6 rounded-xl bg-slate-50 p-4"><p class="text-xs font-semibold uppercase tracking-wide text-slate-400">Catatan</p><p class="mt-1 text-sm text-slate-600">{{ $transaction->catatan }}</p></div>@endif</div><div class="space-y-6"><div class="card p-6"><h2 class="font-bold text-slate-900">Pembayaran</h2><p class="mt-4"><x-status-badge :status="$transaction->status_pembayaran"/></p>@if ($transaction->status_pembayaran === \App\Enums\PaymentStatus::BelumDibayar)<a href="{{ route('customer.payments.show', $transaction) }}" class="btn-primary mt-5 w-full">Bayar sekarang</a>@elseif ($transaction->payment)<p class="mt-4 text-sm text-slate-500">{{ $transaction->payment->metode->label() }} · {{ $transaction->payment->tanggal_bayar?->format('d M Y, H:i') }}</p><a href="{{ route('customer.payments.show', $transaction) }}" class="mt-4 inline-block text-sm font-semibold text-brand-600">Lihat detail →</a>@endif</div><div class="card p-6"><h2 class="font-bold text-slate-900">Ulasan</h2>@if ($transaction->review)<p class="mt-3 text-xl text-brand-500">{{ str_repeat('★', $transaction->review->bintang) }}<span class="text-slate-200">{{ str_repeat('★', 5 - $transaction->review->bintang) }}</span></p><p class="mt-2 text-sm text-slate-500">{{ $transaction->review->ulasan }}</p>@elseif ($transaction->canBeReviewed())<p class="mt-3 text-sm text-slate-500">Laundry selesai? Berikan penilaian Anda.</p><a href="{{ route('customer.reviews.create', $transaction) }}" class="btn-secondary mt-4 w-full">Beri rating</a>@else<p class="mt-3 text-sm text-slate-400">Rating dapat diisi setelah laundry selesai atau diambil.</p>@endif</div></div></div>
@endsection
