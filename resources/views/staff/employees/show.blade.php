@extends('layouts.dashboard')

@section('title', $employee->nama_lengkap)
@section('header', 'Detail karyawan')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('admin.karyawan.index') }}" class="text-sm font-semibold text-brand-600">← Kembali ke karyawan</a><h1 class="page-title mt-3">{{ $employee->nama_lengkap }}</h1><p class="page-subtitle">Detail akun dan transaksi karyawan.</p></div><a href="{{ route('admin.karyawan.edit', $employee) }}" class="btn-secondary">Edit karyawan</a></div><div class="grid gap-6 lg:grid-cols-3"><div class="card p-6"><div class="flex items-center gap-4"><span class="flex h-16 w-16 items-center justify-center rounded-full bg-brand-100 text-2xl font-bold text-brand-700">{{ strtoupper(substr($employee->nama_lengkap, 0, 1)) }}</span><div><h2 class="font-bold text-slate-900">{{ $employee->nama_lengkap }}</h2><p class="text-sm text-slate-500">{{ $employee->posisi_jabatan }}</p></div></div><dl class="mt-6 space-y-4 text-sm"><div><dt class="text-xs text-slate-400">Username</dt><dd class="mt-1 font-semibold text-slate-700">{{ $employee->username }}</dd></div><div><dt class="text-xs text-slate-400">Email</dt><dd class="mt-1 font-semibold text-slate-700">{{ $employee->email }}</dd></div><div><dt class="text-xs text-slate-400">No. HP</dt><dd class="mt-1 font-semibold text-slate-700">{{ $employee->no_hp }}</dd></div><div><dt class="text-xs text-slate-400">Total transaksi</dt><dd class="mt-1 text-xl font-bold text-brand-600">{{ $employee->transactions_count }}</dd></div></dl></div><div class="card table-wrap lg:col-span-2"><div class="border-b border-slate-100 px-5 py-5"><h2 class="font-bold text-slate-900">Transaksi Ditangani</h2></div><table class="data-table"><thead><tr><th>Kode</th><th>Pelanggan</th><th>Layanan</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@forelse($employee->transactions as $transaction)<tr><td class="font-semibold text-brand-700">{{ $transaction->kode_transaksi }}</td><td>{{ $transaction->user->nama_lengkap }}</td><td>{{ $transaction->layanan->nama_layanan }}</td><td>Rp{{ number_format((float) $transaction->total_harga, 0, ',', '.') }}</td><td><x-status-badge :status="$transaction->status_laundry"/></td><td><a href="{{ route('admin.transactions.show', $transaction) }}" class="font-semibold text-brand-600">Detail</a></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-slate-400">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div></div>
@endsection
