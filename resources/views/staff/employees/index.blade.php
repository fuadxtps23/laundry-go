@extends('layouts.dashboard')

@section('title', 'Data Karyawan')
@section('header', 'Data karyawan')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="page-title">Data karyawan</h1><p class="page-subtitle">Kelola akun dan akses tim operasional.</p></div><a href="{{ route('admin.karyawan.create') }}" class="btn-primary">＋ Tambah karyawan</a></div><form method="GET" class="card mb-6 flex flex-col gap-3 p-4 sm:flex-row"><div class="flex-1"><label class="form-label" for="search">Cari karyawan</label><input id="search" name="search" value="{{ $search }}" class="form-input" placeholder="Nama, username, atau email"></div><div class="flex items-end"><button class="btn-primary">Cari</button></div></form><div class="card table-wrap"><table class="data-table"><thead><tr><th>Nama</th><th>Username</th><th>Jabatan</th><th>Email</th><th>Transaksi</th><th>Aksi</th></tr></thead><tbody>@forelse($employees as $employee)<tr><td><span class="flex items-center gap-3"><span class="flex h-9 w-9 items-center justify-center rounded-full bg-brand-100 text-xs font-bold text-brand-700">{{ strtoupper(substr($employee->nama_lengkap, 0, 1)) }}</span><span class="font-semibold text-slate-800">{{ $employee->nama_lengkap }}</span></span></td><td>{{ $employee->username }}</td><td>{{ $employee->posisi_jabatan }}</td><td>{{ $employee->email }}</td><td>{{ $employee->transactions_count }}</td><td><div class="flex items-center gap-3"><a href="{{ route('admin.karyawan.show', $employee) }}" class="font-semibold text-brand-600">Detail</a><a href="{{ route('admin.karyawan.edit', $employee) }}" class="font-semibold text-slate-500">Edit</a><form method="POST" action="{{ route('admin.karyawan.destroy', $employee) }}" onsubmit="return confirm('Hapus karyawan ini?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="6" class="py-14 text-center text-slate-400">Karyawan tidak ditemukan.</td></tr>@endforelse</tbody></table></div>@if($employees->hasPages())<div class="mt-5">{{ $employees->links() }}</div>@endif
@endsection
