@extends('layouts.dashboard')

@section('title', 'Edit Layanan')
@section('header', 'Edit layanan')

@section('content')
<div class="mb-8"><a href="{{ route($role.'.services.index') }}" class="text-sm font-semibold text-brand-600">← Kembali ke layanan</a><h1 class="page-title mt-3">Edit {{ $layanan->nama_layanan }}</h1><p class="page-subtitle">Perbarui informasi layanan laundry.</p></div><form method="POST" action="{{ route($role.'.services.update', $layanan) }}" class="max-w-3xl">@csrf @method('PUT')<div class="card p-6 sm:p-8"><div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label class="form-label" for="nama_layanan">Nama layanan</label><input id="nama_layanan" name="nama_layanan" value="{{ old('nama_layanan', $layanan->nama_layanan) }}" class="form-input" required>@error('nama_layanan')<p class="form-error">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label class="form-label" for="deskripsi">Deskripsi</label><textarea id="deskripsi" name="deskripsi" rows="3" class="form-input">{{ old('deskripsi', $layanan->deskripsi) }}</textarea>@error('deskripsi')<p class="form-error">{{ $message }}</p>@enderror</div><div><label class="form-label" for="harga_per_kg">Harga per satuan</label><input id="harga_per_kg" type="number" step="0.01" min="0" name="harga_per_kg" value="{{ old('harga_per_kg', $layanan->harga_per_kg) }}" class="form-input" required>@error('harga_per_kg')<p class="form-error">{{ $message }}</p>@enderror</div><div><label class="form-label" for="satuan">Satuan</label><select id="satuan" name="satuan" class="form-input" required>@foreach(['kg'=>'Kilogram (kg)','pasang'=>'Pasang','potong'=>'Potong','m2'=>'Meter persegi (m²)'] as $value=>$label)<option value="{{ $value }}" @selected(old('satuan', $layanan->satuan) === $value)>{{ $label }}</option>@endforeach</select>@error('satuan')<p class="form-error">{{ $message }}</p>@enderror</div><div><label class="form-label" for="estimasi_hari">Estimasi hari</label><input id="estimasi_hari" type="number" min="1" max="30" name="estimasi_hari" value="{{ old('estimasi_hari', $layanan->estimasi_hari) }}" class="form-input" required>@error('estimasi_hari')<p class="form-error">{{ $message }}</p>@enderror</div><label class="flex items-center gap-3 self-end rounded-xl border border-slate-200 p-3"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $layanan->is_active)) class="rounded border-slate-300 text-brand-500 focus:ring-brand-400"><span class="text-sm font-semibold text-slate-700">Layanan aktif</span></label></div><div class="mt-7 flex justify-end gap-3"><a href="{{ route($role.'.services.index') }}" class="btn-secondary">Batal</a><button class="btn-primary">Simpan perubahan</button></div></div></form>
@endsection
