@extends('layouts.dashboard')

@section('title', 'Data Layanan')
@section('header', 'Data layanan')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="page-title">Data layanan</h1><p class="page-subtitle">Atur katalog, harga, dan status aktif layanan.</p></div><a href="{{ route($role.'.services.create') }}" class="btn-primary">＋ Tambah layanan</a></div><div class="card table-wrap"><table class="data-table"><thead><tr><th>Layanan</th><th>Harga</th><th>Estimasi</th><th>Transaksi</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($services as $service)<tr><td><p class="font-semibold text-slate-800">{{ $service->nama_layanan }}</p><p class="max-w-xs truncate text-xs text-slate-400">{{ $service->deskripsi }}</p></td><td class="font-semibold text-brand-600">Rp{{ number_format((float) $service->harga_per_kg, 0, ',', '.') }}<span class="text-xs font-normal text-slate-400">/{{ $service->satuan }}</span></td><td>{{ $service->estimasi_hari }} hari</td><td>{{ $service->transactions_count }}</td><td><span class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold {{ $service->is_active ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-100 text-slate-600' }}">{{ $service->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="flex items-center gap-3"><a href="{{ route($role.'.services.edit', $service) }}" class="font-semibold text-brand-600">Edit</a><form method="POST" action="{{ route($role.'.services.destroy', $service) }}" onsubmit="return confirm('Hapus layanan ini?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="6" class="py-14 text-center text-slate-400">Belum ada layanan.</td></tr>@endforelse</tbody></table></div>@if($services->hasPages())<div class="mt-5">{{ $services->links() }}</div>@endif
@endsection
