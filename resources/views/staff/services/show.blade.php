@extends('layouts.dashboard')

@section('title', $service->nama_layanan)
@section('header', 'Detail layanan')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route($role.'.services.index') }}" class="text-sm font-semibold text-brand-600">← Kembali ke layanan</a><h1 class="page-title mt-3">{{ $service->nama_layanan }}</h1><p class="page-subtitle">Informasi layanan dan statistik transaksi.</p></div><a href="{{ route($role.'.services.edit', $service) }}" class="btn-secondary">Edit layanan</a></div><div class="grid gap-6 lg:grid-cols-2"><div class="card p-6"><h2 class="font-bold text-slate-900">Detail layanan</h2><dl class="mt-6 space-y-4 text-sm"><div><dt class="text-xs text-slate-400">Deskripsi</dt><dd class="mt-1 text-slate-600">{{ $service->deskripsi ?: 'Tidak ada deskripsi.' }}</dd></div><div><dt class="text-xs text-slate-400">Harga</dt><dd class="mt-1 text-xl font-bold text-brand-600">Rp{{ number_format((float) $service->harga_per_kg, 0, ',', '.') }}<span class="text-sm font-medium text-slate-400">/{{ $service->satuan }}</span></dd></div><div><dt class="text-xs text-slate-400">Estimasi selesai</dt><dd class="mt-1 font-semibold text-slate-700">{{ $service->estimasi_hari }} hari</dd></div><div><dt class="text-xs text-slate-400">Status</dt><dd class="mt-1"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $service->is_active ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-100 text-slate-600' }}">{{ $service->is_active ? 'Aktif' : 'Nonaktif' }}</span></dd></div></dl></div><div class="card p-6"><h2 class="font-bold text-slate-900">Statistik</h2><p class="mt-6 text-4xl font-bold text-brand-600">{{ $service->transactions_count }}</p><p class="mt-2 text-sm text-slate-500">transaksi menggunakan layanan ini</p></div></div>
@endsection
