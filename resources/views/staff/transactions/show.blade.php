@extends('layouts.dashboard')

@section('title', $transaction->kode_transaksi)
@section('header', 'Detail transaksi')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route($role.'.transactions.index') }}" class="text-sm font-semibold text-brand-600">← Kembali ke transaksi</a><h1 class="page-title mt-3">{{ $transaction->kode_transaksi }}</h1><p class="page-subtitle">Detail pesanan pelanggan {{ $transaction->user->nama_lengkap }}.</p></div><div class="flex gap-2"><a href="{{ route($role.'.transactions.edit', $transaction) }}" class="btn-secondary">Edit data</a><form method="POST" action="{{ route($role.'.transactions.destroy', $transaction) }}" onsubmit="return confirm('Hapus transaksi ini?')">@csrf @method('DELETE')<button class="btn-danger">Hapus</button></form></div></div>
<div class="grid gap-6 lg:grid-cols-3"><div class="card p-6 lg:col-span-2"><h2 class="font-bold text-slate-900">Informasi transaksi</h2><dl class="mt-6 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Pelanggan</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->user->nama_lengkap }}</dd><dd class="text-xs text-slate-400">{{ $transaction->user->no_hp }} · {{ $transaction->user->email }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Layanan</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->layanan->nama_layanan }}</dd><dd class="text-xs text-slate-400">{{ $transaction->layanan->satuan }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Berat / jumlah</dt><dd class="mt-1 font-semibold text-slate-800">{{ (float) $transaction->berat }} {{ $transaction->layanan->satuan }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Total harga</dt><dd class="mt-1 text-lg font-bold text-brand-600">Rp{{ number_format((float) $transaction->total_harga, 0, ',', '.') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Tanggal masuk</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->tanggal_masuk->format('d F Y') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Estimasi selesai</dt><dd class="mt-1 font-semibold text-slate-800">{{ $transaction->tanggal_estimasi_selesai->format('d F Y') }}</dd></div></dl>@if($transaction->catatan)<div class="mt-6 rounded-xl bg-slate-50 p-4"><p class="text-xs font-semibold uppercase tracking-wide text-slate-400">Catatan pelanggan</p><p class="mt-1 text-sm text-slate-600">{{ $transaction->catatan }}</p></div>@endif</div><div class="space-y-6"><div class="card p-6"><h2 class="font-bold text-slate-900">Status laundry</h2><div class="mt-4"><x-status-badge :status="$transaction->status_laundry"/></div>@if($transaction->status_laundry->next())<form method="POST" action="{{ route($role.'.transactions.status', $transaction) }}" class="mt-5">@csrf<label class="form-label" for="status_laundry">Ubah ke status berikutnya</label><select id="status_laundry" name="status_laundry" class="form-input"><option value="{{ $transaction->status_laundry->next()->value }}">{{ $transaction->status_laundry->next()->label() }}</option></select><button class="btn-primary mt-3 w-full">Perbarui status</button></form>@else<p class="mt-4 text-xs text-slate-400">Status sudah berjalan sampai selesai.</p>@endif</div><div class="card p-6"><h2 class="font-bold text-slate-900">Pembayaran</h2><div class="mt-4"><x-status-badge :status="$transaction->status_pembayaran"/></div>@if($transaction->payment)<p class="mt-3 text-sm text-slate-500">{{ $transaction->payment->metode->label() }} · Rp{{ number_format((float) $transaction->payment->jumlah_bayar, 0, ',', '.') }}</p>@if($transaction->payment->bukti_pembayaran)<a href="{{ Storage::disk('public')->url($transaction->payment->bukti_pembayaran) }}" target="_blank" class="mt-3 inline-block text-sm font-semibold text-brand-600">Lihat bukti →</a>@endif@else<p class="mt-3 text-sm text-slate-400">Belum ada pembayaran.</p>@endif</div>@if($role === 'admin')<div class="card p-6"><h2 class="font-bold text-slate-900">Karyawan penanggung jawab</h2><form method="POST" action="{{ route('admin.transactions.assign', $transaction) }}" class="mt-4">@csrf<select name="karyawan_id" class="form-input" required><option value="">Pilih karyawan</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected($transaction->karyawan_id === $employee->id)>{{ $employee->nama_lengkap }} · {{ $employee->posisi_jabatan }}</option>@endforeach</select><button class="btn-primary mt-3 w-full">Simpan penugasan</button></form></div>@endif</div></div>
@endsection
