<!DOCTYPE html>
<html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Laporan Laundry Go</title><style>body{font-family:Arial,sans-serif;color:#1e293b;margin:32px}header{border-bottom:3px solid #f59e0b;padding-bottom:16px;margin-bottom:24px}h1{margin:0;font-size:24px}header p{margin:6px 0;color:#64748b;font-size:13px}.summary{display:flex;gap:24px;margin:20px 0}.summary div{padding:12px 16px;background:#fff7ed;border-radius:8px}.summary b{display:block;font-size:20px;color:#b45309}table{width:100%;border-collapse:collapse;font-size:12px}th,td{border:1px solid #e2e8f0;padding:8px;text-align:left}th{background:#f8fafc}footer{margin-top:28px;font-size:11px;color:#94a3b8}@media print{button{display:none}}</style></head><body><header><h1>Laporan Laundry Go</h1><p>Periode {{ $from->format('d M Y') }} — {{ $to->format('d M Y') }} · {{ ucfirst($period) }}</p><p>Dicetak oleh {{ $staffUser->nama_lengkap }} pada {{ now()->format('d M Y H:i') }}</p></header><div class="summary"><div><span>Transaksi</span><b>{{ $transactions->count() }}</b></div><div><span>Pendapatan</span><b>Rp{{ number_format($revenue, 0, ',', '.') }}</b></div></div><table><thead><tr><th>Kode</th><th>Tanggal</th><th>Pelanggan</th><th>Layanan</th><th>Total</th><th>Status</th><th>Pembayaran</th></tr></thead><tbody>@foreach($transactions as $transaction)<tr><td>{{ $transaction->kode_transaksi }}</td><td>{{ $transaction->tanggal_masuk->format('d/m/Y') }}</td><td>{{ $transaction->user->nama_lengkap }}</td><td>{{ $transaction->layanan->nama_layanan }}</td><td>Rp{{ number_format((float) $transaction->total_harga, 0, ',', '.') }}</td><td>{{ $transaction->status_laundry->label() }}</td><td>{{ $transaction->status_pembayaran->label() }}</td></tr>@endforeach</tbody></table>@if($role === 'admin')<h2 style="font-size:16px;margin-top:24px">Rekap per karyawan</h2><table><thead><tr><th>Karyawan</th><th>Jabatan</th><th>Transaksi</th><th>Pendapatan</th></tr></thead><tbody>@foreach($employeeRecap as $recap)<tr><td>{{ $recap['employee']->nama_lengkap }}</td><td>{{ $recap['employee']->posisi_jabatan }}</td><td>{{ $recap['count'] }}</td><td>Rp{{ number_format($recap['revenue'], 0, ',', '.') }}</td></tr>@endforeach</tbody></table>@endif<footer>Laporan dibuat oleh sistem Laundry Go.</footer></body></html>
